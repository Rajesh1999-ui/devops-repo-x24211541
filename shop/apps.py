"""App configuration for shop app."""
from django.apps import AppConfig


class ShopConfig(AppConfig):
    """Configuration class for shop app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'shop'
    